# 设计验收记录

## 比较目标

- 源视觉真值：`D:\Tools\codex\generated_images\01a0808b-7872-7751-848f-6a1a5c7e4432\exec-0ec828f1-86f6-4166-af2c-f27f407993da.png`
- 最终实现截图：`D:\Tools\codex\visualizations\2026\09\08\01a0808b-7872-7751-848f-6a1a5c7e4432\quadrat-lab-implementation.png`
- 源图尺寸：1684 × 936；实现视口与截图：1646 × 912，`deviceScaleFactor: 1`。
- 对照状态：草原场景、已框选一个 1m × 1m 样方、200% 教学缩放、右侧显示当前样方统计。

源图与实现截图已在同一视觉检查输入中并排查看。两者宽高比一致，仅按各自视口做等比例比较，没有使用会改变布局比例的非等比拉伸。

## 全视图与重点区域

- 全视图：顶部深绿色导航、窄版场景/标本栏、独立深绿色工具栏、中央草原画布和右侧统计栏的层级、列宽与留白均与确认稿一致；底部冗余流程已移除。
- 左侧：场景选择与植物标本压缩为紧凑卡片，无垂直滚动条，信息仍能一屏完整读取。
- 工具栏：光标、框选、拖动画布、辅助线、缩放、重置、撤销、清空成为独立纵向工具栏，选中态清晰。
- 中央画布：保持既有草地逻辑；200% 下 1m 样方按教学比例放大，橙色高亮限制在样方内部，密度标签固定在样方右上角附近。
- 右侧：统计卡、样方记录、平均密度与主要操作维持同一列对齐，不再依赖底部状态区。

## 五项保真检查

- 字体与排版：沿用项目中文系统字体栈；标题、统计数字、工具标签与说明文字层级清楚，未出现截断或错位。
- 间距与布局：1646 × 912 主验收视口采用约 224px / 94px / 自适应画布 / 410px 四栏；1280 × 800 使用紧凑列宽，所有核心控件仍在首屏内。
- 颜色与视觉令牌：森林绿、米白、鼠尾草绿、琥珀橙和蓝色辅助色与确认稿一致；选中、禁用和高亮状态语义明确。
- 图片与素材：继续复用项目现有裸土、植物和顶部纹理资产；高倍缩放时植物改为清晰图片节点渲染，土壤改用平铺背景，避免整体位图放大造成模糊。
- 页面文案：场景名称、尺寸、取样法、工具名称、样方记录与统计单位保持一致；动态株数由当前框选区域决定，因此允许与静态确认稿不同。

## 迭代历史

1. P1：旧布局左侧图例过宽、工具混在图例底部，且底部流程占用高度。修复为窄图例栏、独立 PS 风格工具栏，并移除底部流程。
2. P1：旧交互点击即生成样方，无法明确表达“框选”。新增框选工具与拖拽反馈，释放鼠标时生成固定 1m × 1m 样方。
3. P1：缩放后样方仍显得过小，且不能平移。引入教学缩放倍率，并增加独立拖动画布工具；样方、植物和场景按同一视觉尺度变化。
4. P1：高倍缩放时土壤和植物发糊。草原土壤改为高倍平铺渲染，视口附近植物使用原始图片节点绘制，物种形态可一眼区分。
5. P2：早期版本的密度标签、虚线和目标植物外圈会随场景过度放大或溢出样方。修复为固定可读标签、按倍率折算虚线，并把橙色高亮严格限制在样方内部。
6. P2：左右操作区在部分视口中未对齐。重构四栏网格并在 1646 × 912、1280 × 800 两个桌面尺寸复核。

## 允许的差异

- P3：植物分布与样方株数来自当前随机场景和框选位置，和静态确认稿中的示例数字可以不同。
- P3：源图为 1684 × 936，实现主验收视口为 1646 × 912；两者比例接近，未发现由尺寸差异引起的结构偏移。
- 当前没有待处理的 P0、P1 或 P2 视觉差异。

## 浏览器验证

- 页面身份与首屏：`http://127.0.0.1:1420/` 正常加载，无空白页、遮罩层或错误页。
- 控制台：最终浏览器检查无 error 或 warning。
- 核心交互：框选固定 1m × 1m 样方、内部高亮、右上角株数/㎡、200% 缩放、拖动画布、辅助线、撤销和清空均可用。
- 响应式：1646 × 912 与 1280 × 800 均无核心内容裁切或意外滚动条。
- 构建：`pnpm run build` 通过。
- 端到端测试：`pnpm exec playwright test`，6/6 通过。
- 单元测试：`pnpm exec vitest run --testTimeout 15000`，11/11 通过。

final result: passed
