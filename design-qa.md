# 设计验收记录

## 比较目标

- 源视觉真值（绿化带）：`C:\Users\89589\.codex\generated_images\01a07e77-4bfc-74d3-8db6-af28cbb8b312\exec-96fb790a-4fb2-4d14-b6b1-bd467a89b6a8.png`
- 源视觉真值（草原）：`C:\Users\89589\.codex\generated_images\01a07e77-4bfc-74d3-8db6-af28cbb8b312\exec-a309c9f8-3e1e-48e8-b245-d35d52c60f7f.png`
- 实现截图（绿化带）：`C:\Users\89589\Desktop\code\quadrat-lab\design-qa-greenbelt.png`
- 实现截图（草原）：`C:\Users\89589\Desktop\code\quadrat-lab\design-qa-grassland.png`
- 同尺寸对照图（绿化带）：`C:\Users\89589\Desktop\code\quadrat-lab\design-qa-greenbelt-comparison.png`
- 同尺寸对照图（草原）：`C:\Users\89589\Desktop\code\quadrat-lab\design-qa-grassland-comparison.png`

源图与实现截图均为 1920 × 1080 像素；实现使用 CSS viewport 1920 × 1080、`deviceScaleFactor: 1`，未做密度归一化。对照图将同一场景的源图和实现截图横向拼接后再进行判断。

## 状态与证据

- 绿化带：完成双点取样、自动锁定间距、标准铺满，最终 6 个样方；样方 3 选中并完成计数、平均值计算。
- 草原：放置中心样方、开启 X 型辅助线、吸附四角样方，最终 5 个样方；样方 5 选中并完成计数、平均值计算。
- 全视图证据：两张横向对照图确认深绿色顶部导航、左侧标本栏、中央场景、右侧统计检查器、底部状态栏的层级和比例一致，且 1920 × 1080 下无持久控件被裁切。
- 评论复核：草原与绿化带采用完整写实场景底图，避免将场景降级为 SVG 示意图；底图仅提供连续材质，Canvas 动态植物层根据当前随机分布绘制可变化植株，SVG 继续承载网格、样方、辅助线和点击交互。
- 重点区域证据：对照图重点检查了绿化带的乔木带/上边界/种植区/下边界/道路五层关系，以及草原的正方形样地、X 型辅助线和五点样方；不再需要额外裁剪，重点区域在同尺寸对照图中可清晰判断。

## 五项保真面检查

- 字体与排版：使用系统中文无衬线回退栈，标题、场景信息、统计数字和辅助说明形成明确层级；小字号说明在 1920 × 1080 视口内未出现截断。
- 间距与布局节奏：采用 300px / 主画布 / 360px 三栏网格，顶部导航、主工作区和底部状态区固定分层；画布使用 `min-height: 0` 与弹性布局，已修复低高度预览时底部说明被挤出的早期问题。
- 颜色与视觉令牌：森林绿、米白、鼠尾草绿、琥珀橙和蓝色辅助线统一收敛为页面令牌；主要操作、选中态、禁用态和统计结果有明确语义区分。
- 图片质量与素材保真：已接入草原与绿化带完整场景底图、顶部植物纹理，以及茵陈蒿、狗尾巴草、小草、蒲公英、鸢尾花独立 PNG；左侧标本栏继续使用独立素材，中央画布由写实底图和轻量交互叠加层组成。
- 页面文案：草原仅展示茵陈蒿、狗尾巴草、小草；绿化带展示蒲公英、鸢尾花、狗尾巴草；尺寸、面积、边界规则、五点取样法和等距取样提示均已纳入新布局。

## 迭代历史

1. P2：早期实现中画布 flex 高度约束不足，低高度窗口会挤出比例尺和底部说明。修复：为画布壳层和 SVG 增加 `min-height: 0`、`flex: 1 1 auto`，并重新在 1920 × 1080 视口截图确认底部区域完整。
2. P1：早期实现中的植物为内联手绘路径，和设计稿标本质感及用户要求的“对应图片”不一致。修复：生成五种透明 PNG，切分为独立资产并同时接入标本卡和画布；重新截图后确认物种可区分、透明背景正常。
3. P1/P2（本轮浏览器评论）：草原斑块之间出现空区、植物难辨、图例素材存在接触表裁切问题。修复：使用分区覆盖算法补齐空间分布，替换为五张独立透明 PNG，并对画布植物层增加场景裁切；图例卡片在常规与低高度视口下进一步压缩。
4. P1（本轮视觉重构）：原 SVG 视口按 `meet` 缩放导致中央场景缩小，且草地、树木、道路为代码示意图形。修复：引入完整底图，草原使用正方形画板，绿化带使用横向全宽画板，SVG 只做交互覆盖。
5. 当前复核：未发现可阻塞交付的 P0/P1/P2 差异。植物标本与画布使用同一组独立 PNG，物种对应关系一致。
6. P1（静态场景反馈）：仅使用完整底图会让“重新生成植物”缺少可见反馈。修复：新增 `PlantDistributionLayer` Canvas 层，复用逻辑生成器的植物点位、大小和角度；核心植物使用更清晰的独立尺度、物种色标和刷新过渡，每次重新生成随机种子后，场景中的动态植株随之变化，统计仍使用完整植物点集。
7. P1（物种变化反馈）：明确限制草原动态层为茵陈蒿/狗尾巴草/小草，绿化带动态层为鸢尾花/蒲公英/狗尾巴草；放大真实植株素材、裁掉根系、增加物种色轮廓并压低底图固定植被。绿化带按高株、花丛、前景三层布置，鸢尾花改为真正随机点位，六个场景-物种组合均增加随机种子回归测试。
8. P2（场景切换反馈）：两张大场景图改为进入页面即同时预载，通过显隐切换，避免首次切换时短暂残留上一场景。
9. P1（绿化带背景与动态区域反馈）：使用图片编辑生成新的裸土绿化带底图，保留乔木带、上下石质边界和道路，移除种植区全部固定植物；动态区域改为与裸土边界严格一致。鸢尾花采用 3 行 × 11 列、轻微随机错位的横向铺满布局，蒲公英和狗尾巴草继续使用不规则随机分布。

## 浏览器验证

- 自动化浏览器用例：3 passed（包含动态植物层重新生成校验）。
- 单元测试：10 passed（包含鸢尾花三行横向铺满与六个场景-物种随机变化校验）。
- 类型检查：`pnpm exec vue-tsc --noEmit` 通过。
- 控制台：通过浏览器控制台检查，未发现 error 或 warning。
- 已验证交互：场景切换、绿化带双点定距、标准铺满、草原中心样方、X 型辅助线、四角吸附、标记计数、平均值计算。
- Tauri：按用户要求本轮未构建。

## Implementation Checklist

- [x] 1920 × 1080 桌面布局
- [x] 草原五点取样与 X 型辅助线
- [x] 绿化带树木边界与上下边界层次
- [x] 绿化带第三种植物为狗尾巴草
- [x] 五种植物图片资产接入画布和标本栏
- [x] 草原与绿化带写实场景底图
- [x] 绿化带裸土底图与动态种植区边界严格对齐
- [x] 鸢尾花三行横向铺满，蒲公英与狗尾巴草不规则分布
- [x] Canvas 动态植物层随重新生成改变分布
- [x] SVG 交互层与场景底图分离
- [x] 草原植物分区覆盖与场景裁切
- [x] 图例卡片压缩并在 1920 × 1080 浏览器视口复核
- [x] 浏览器自动化和单元测试通过

final result: passed
